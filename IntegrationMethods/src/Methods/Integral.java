package Methods;

public interface Integral {
	
	double integrate(double a, double b, int n, Function function);
	/** la siguente interfaz se encarga de definr el prototipo de la funcion que se implementara 
	 * en todas las clases que contendran los distintos metodos de integracions */
	
}
